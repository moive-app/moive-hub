# CLAUDE.md

이 문서는 Claude Code가 MOIVE Hub(`moive-hub`) 레포에서 작업할 때 따라야 하는 **실행 지침**입니다.

문서 작성 규칙, 정보 신뢰도 원칙, 상태값, 원본 시스템(Source of Truth) 정의, 보안·개인정보 원칙 등 **레포의 규범 자체는 [AGENTS.md](AGENTS.md)가 정본**입니다. 이 문서는 그 규범을 반복하지 않고, "Claude가 실제로 어떤 순서로, 어떤 조건에서, 무엇을 하고 하지 말아야 하는가"만 다룹니다. 두 문서가 다른 말을 하는 것처럼 보이면 AGENTS.md를 기준으로 판단하고, 그 판단을 사용자에게 알립니다.

## 1. 작업 시작 시 기본 행동

작업을 시작하기 전 다음을 이 순서로 확인합니다.

1. `CLAUDE.md` (이 문서)
2. `AGENTS.md`
3. `README.md`
4. 작업 대상과 직접 관련된 `docs/` 하위 문서
5. 필요한 경우 문서에 연결된 원본(Notion/Discord/Figma/GitHub) 링크

문서 간 충돌이 발견되면 임의로 해결하지 않고, 충돌 내용과 각 문서의 상태·날짜를 밝혀 사용자에게 보고합니다.

## 2. MOIVE의 실제 운영 구조

레포 문서(README.md, AGENTS.md)는 Notion/Discord/Figma/GitHub를 원본 시스템으로 정의합니다. 각 시스템의 실제 구성은 다음과 같습니다.

### 결정이 만들어지는 흐름

Discord Forum/Thread → 기능별 논의 → 기획자 최종 결정 → Notion에 최종 결정 내용 반영 → 필요한 경우 MOIVE Hub의 `docs/decisions/`에 결정 기록 및 원본 링크 연결

### Discord — 기능별 논의의 원본
기능별 Forum 안에서 Thread 단위로 질문·의견·문제 제기·대안 검토 등의 논의가 이루어집니다. 논의 내용 자체는 확정된 사실이 아닙니다.

### Notion — 최종 확정 기획·정책의 공식 문서
다음 영역이 존재합니다.

- 팀 프로필, 회의록, 최종 선정 아이디어, 마일스톤, 계정 정보, 리브데이 관련, 유저 리서치 분석 보고서, 데스크 리서치 분석 보고서
- 기획: 진행 상황, 기획 문서, 기능 명세서, 정책집
- 디자인: 진행 상황
- 프론트엔드: Android
- 백엔드: API 명세서, 백엔드, 개발 공유보고서, 보안 관련

### Figma — UI/UX 디자인의 원본
현재 실제 화면 디자인은 아직 없습니다.

### GitHub — 레포별 역할
- `moive-android`: MOIVE 안드로이드 앱 프론트엔드 개발 레포
- `moive-server`: MOIVE Spring Boot 백엔드/API 서버 레포
- `moive-terms`: MOIVE 이용약관, 개인정보처리방침 등 법적 고지 페이지를 관리하는 레포
- `moive-hub` (현재 레포): MOIVE 팀의 지식, 문서, 결정 기록 및 관련 원본 링크를 관리하는 레포

moive-hub는 개발 레포의 코드를 직접 보관하지 않고, 필요한 경우 각 개발 레포의 Issue/PR 등 관련 링크를 연결하는 역할을 합니다.

## 3. 현재 프로젝트 상태 — 초기 단계

MOIVE는 현재 초기 기획 단계입니다.

- PRD는 아직 작성되지 않았습니다.
- 기능별 상세 기획 중 아직 작성되지 않은 항목이 있습니다.
- 실제 화면 디자인과 Figma 링크가 아직 없다는 사실은 2절의 Figma 설명과 같습니다.

`docs/design/screens.md`를 비롯한 Hub의 빈 문서·`미정` 상태는 실제로 아직 정해지지 않았다는 사실을 그대로 반영한 것입니다. 이를 임의의 실제 데이터로 채우지 않습니다.

## 4. 정보가 없거나 충돌할 때의 행동

- 문서나 원본에 정보가 없으면 `미정` 또는 `미연결`로 표시합니다. 빈 값을 채우기 위해 그럴듯한 값을 만들지 않습니다.
- Discord에서 논의 중인 내용을 최종 결정으로 판단하지 않습니다.
- Notion의 최종 확정 내용과 Discord 논의가 충돌하면 임의로 판단하지 않고, 충돌 사실을 그대로 사용자에게 보고합니다.
- 다음 정보는 절대로 추측해서 생성하지 않습니다.
  - 기능, 화면, 사용자 플로우, 정책, 요구사항
  - 담당자, 일정
  - Figma URL, Notion URL, Discord URL, GitHub Issue/PR
  - API 정보
  - 최종 의사결정

## 5. 문서·파일 수정 전 승인 절차

Claude가 문서나 설정 파일을 수정할 필요가 있다고 판단하더라도 바로 수정하지 않습니다. 다음 순서를 따릅니다.

1. 현재 문서와 관련 원본을 확인한다.
2. 변경이 필요한 이유를 설명한다.
3. 변경안을 제안한다.
4. 사용자 승인을 기다린다.
5. 사용자가 명시적으로 승인한 경우에만 파일을 수정한다.

사용자가 처음부터 명시적으로 수정을 요청한 경우에는 해당 요청 범위 내에서 바로 수정할 수 있습니다. 요청 범위를 벗어나는 추가 수정(예: 요청받지 않은 다른 문서 재구성)은 별도로 제안하고 승인을 받습니다.

문서 수정이 필요한 경우 관련 문서 간 참조 관계를 함께 확인합니다. 한 문서의 변경으로 다른 문서(예: `screens.md`, 의사결정 기록)가 잘못된 상태가 되면, 관련 문서도 함께 수정할 필요가 있는지 먼저 설명합니다. 단순히 내용을 맞추기 위해 기존 문서를 임의로 삭제하거나 대규모로 재구성하지 않습니다.

## 6. 보안

보안·개인정보 관련 원칙은 [AGENTS.md](AGENTS.md)를 따른다.
