# MOIVE Knowledge Hub 운영 가이드

이 문서는 이 저장소의 문서를 읽거나 작성하는 사람과 AI를 위한 공통 규칙입니다.
목표는 최신의 승인된 정보를 쉽게 찾고, 근거와 변경 이력을 잃지 않으며, 불확실한 정보를 사실처럼 다루지 않는 것입니다.

## 저장소의 역할

- Discord Forum/Thread는 기능별 질문, 의견, 논의, 결정 과정의 원본입니다. 하나의 기능에 대한 미확정 의견과 확정된 결정 과정이 함께 존재할 수 있습니다.
- Notion은 최종 확정된 상세 기획 및 정책의 공식 문서입니다.
- Figma는 시각 디자인의 원본입니다. 이 저장소는 Figma 파일을 복제하지 않고 화면별 링크와 진행 상태, 관련 맥락을 관리합니다.
- GitHub Issues·PR은 실제 구현 및 개발 이력의 원본입니다.
- 이 저장소(MOIVE Hub)는 각 원본을 연결하고, 승인된 핵심 지식을 검색하기 쉽게 관리합니다.

## 정보 신뢰도와 답변 원칙

1. `approved` 상태이며 가장 최근에 검토된 문서를 우선합니다.
2. 문서 간 내용이 충돌하면 임의로 결론을 내리지 않습니다. 충돌하는 문서와 상태·날짜를 밝히고 담당자 확인이 필요하다고 표시합니다.
3. 근거가 없는 제품 정책, 화면명, 링크, 담당자, 일정, 상태를 추측해서 만들지 않습니다.
4. 질문에 답할 때 가능하면 관련 문서 링크를 함께 제시합니다.
5. 변경 전에는 관련 문서, 화면 관리표, 의사결정 기록을 확인합니다. 변경 후에는 상호 참조와 상태를 갱신합니다.
6. Discord Forum/Thread의 미확정 의견·질문·제안은 공식 지식으로 취급하거나 단정해 답하지 않습니다. Notion의 확정 문서 또는 `approved` 의사결정 기록으로 확인된 내용만 현재 기준으로 답합니다.

## 문서 배치 기준

| 경로 | 다루는 내용 |
| --- | --- |
| `docs/_meta/` | 용어집, 담당·검토 책임, 공통 템플릿, 작성 규칙 |
| `docs/product/` | 제품 비전, 로드맵, 요구사항, 지표 |
| `docs/design/` | 디자인 원칙, 화면 상태, 디자인 시스템, UX 리서치 |
| `docs/engineering/` | 아키텍처, 개발 규칙, 운영 절차, ADR |
| `docs/policy/` | 개인정보, 보안, 콘텐츠·운영 정책 |
| `docs/decisions/` | 제품·디자인·기술·운영의 중요한 결정 |
| `docs/meetings/` | 회의 맥락, 논의, 액션 아이템 |
| `docs/archive/` | 폐기되었으나 보존할 문서 |

회의에서 확정된 중요한 결론은 회의록에만 두지 말고 해당 기준 문서 또는 `docs/decisions/`에 반영합니다.

## 문서 작성 규칙

- 기본 언어는 한국어입니다. 고유명사와 코드 식별자는 원문을 유지합니다.
- 파일명은 소문자 kebab-case를 사용합니다. 날짜가 필요하면 `YYYY-MM-DD-주제.md` 형식으로 작성합니다.
- 새 기준 문서에는 가능한 한 다음 메타데이터를 상단에 포함합니다.

```yaml
---
status: draft # draft | proposed | approved | deprecated | archived
owner: 미정
last_reviewed: YYYY-MM-DD
source_of_truth: repository # repository | discord | notion | figma | github
related_links: []
---
```

- 상태의 의미는 다음과 같습니다.
  - `draft`: 작성 중이며 기준으로 사용하지 않습니다.
  - `proposed`: 검토 요청 상태입니다.
  - `approved`: 팀이 합의한 현재 기준입니다.
  - `deprecated`: 더 이상 새 작업의 기준은 아니며 대체 문서를 안내해야 합니다.
  - `archived`: 역사적 기록으로 보존합니다.
- 외부 링크에는 링크의 대상과 역할을 함께 씁니다. 예: `Discord Forum`, `Discord Thread`, `Notion 상세 기획`, `Figma 원본`, `GitHub 구현 이슈`.
- 외부 원본이 기준이라면 `source_of_truth`에 이를 명시하고, 이 저장소에는 요약·링크·검토 상태를 기록합니다.
- 여러 Discord 원본을 연결할 때는 Forum Channel과 개별 Thread를 구분하고 배열 형태로 기록할 수 있습니다.

```yaml
discord_links:
  forums: []
  threads: []
```

## 화면 관리 규칙

- 모든 제품 화면은 `docs/design/screens.md`에 한 행으로 등록합니다.
- 화면별 상세 맥락은 `docs/design/templates/screen.md`를 복제해 별도 문서로 기록할 수 있습니다.
- 화면 관리표의 관련 Discord 논의 링크에는 Forum Channel과 개별 Thread를 구분해 기록합니다.
- Figma 링크만으로 구현 여부를 판단하지 않습니다. 디자인, 상세 기획, 개발, QA 상태를 각각 갱신합니다.
- 화면의 이름이나 URL이 바뀌면 화면 관리표와 관련 문서를 같은 변경에서 함께 갱신합니다.
- 실제 정보가 아직 없다면 `미정` 또는 `미연결`로 표시하며, 가상의 값은 입력하지 않습니다.

## 의사결정과 검토

- 되돌리기 어렵거나 여러 직군에 영향을 주는 선택은 `docs/decisions/`에 기록합니다.
- 기록에는 결정 내용, 배경, 고려한 대안, 영향 범위, 결정일, 결정자, 재검토 조건을 포함합니다.
- Discord에서 결정 과정이 진행된 경우, 결정 기록에는 관련 Discord Forum/Thread 원본 링크를 남깁니다. 최종 상세 기획 또는 정책이 있다면 Notion 공식 문서 링크도 함께 남깁니다.
- Discord의 논의가 있어도 Notion 공식 문서 또는 승인된 결정 기록으로 확정되지 않았다면, 결정 기록을 `approved` 상태로 만들지 않습니다.
- 기준 문서를 승인·폐기할 때는 담당자와 마지막 검토일을 갱신합니다.

## 보안 및 개인정보

- API 키, 비밀번호, 접근 토큰, 비공개 URL의 인증 정보는 절대 기록하지 않습니다.
- 고객·사용자 개인식별정보와 민감한 원문 데이터는 저장하지 않습니다.
- 외부 문서가 접근 권한을 필요로 하는 경우, 링크와 접근 요청 방법만 기록합니다.
